export type SupportData = {
  key: string;
  supportId: string;
  financeNature: string;
  direction: string;
  finInstrument: string;
  estimatedUSD: number;
  estimatedLC: number;
  recievedUSD: number;
  recievedLC: number;
};
